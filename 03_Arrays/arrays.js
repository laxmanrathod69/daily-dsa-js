// 1. Create an number array with 5 elements

const numArray = [1, 2, 3, 4, 5];

console.log(`Number array: ${numArray}`);

// 2. Create a string array with 5 elements

const strArray = ["one", "two", "three", "four", "five"];

console.log(`String array: ${strArray}`);

// 3. Create a boolean array with 5 elements

const boolArray = [true, false, true, false, true];

console.log(`Boolean array: ${boolArray}`);

// 4. Create a mixed array with 5 elements

const mixedArray = [1, "two", true, "four", 5];

console.log(`Mixed array: ${mixedArray}`);
