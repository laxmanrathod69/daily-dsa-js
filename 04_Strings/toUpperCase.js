// Convert the string to Upper case
const toUpperCase = (str) => {
  return str.toUpperCase();
};

const myStr = "apple";
console.log(`String: ${myStr}`);
console.log(`Upper Case: ${toUpperCase(myStr)}`);
