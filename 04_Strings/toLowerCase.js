// Convert the string to Lower case
const toLowerCase = (str) => {
  return str.toLowerCase();
};

const myStr = "LAXMAN RATHOD";
console.log(`String: ${myStr}`);
console.log(`Lower Case: ${toLowerCase(myStr)}`);
